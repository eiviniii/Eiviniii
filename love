<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contagem Regressiva e Galeria</title>
    <style>
        body {
            font-family: sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            background-color: #f0f0f0;
        }
        #contador {
            font-size: 2em;
            margin-bottom: 20px;
        }
        .galeria {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }
        .foto {
            width: 200px;
            height: 200px;
            margin: 10px;
            border: 1px solid #ccc;
            overflow: hidden;
        }
        .foto img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
         .mensagem {
            margin: 10px 0; /* Adiciona
